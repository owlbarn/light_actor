(library
  (name actor_unix)
  (public_name actor_unix)
  (flags (
    ; turn off some warnings as 4.06.0 is pernickety and errors out
    :standard -w "-50" -safe-string)
  )
  (wrapped false)
  (preprocess (pps lwt.ppx))
  (libraries
    lwt
    zmq-lwt
  )
)
